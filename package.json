{
  "name": "coinglass-monitor",
  "version": "1.0.0",
  "description": "CoinGlass 利率监控提醒系统",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "deploy:preview": "wrangler deploy --env preview",
    "tail": "wrangler tail",
    "kv:list": "wrangler kv:key list --namespace-id=your-config-namespace-id",
    "kv:get": "wrangler kv:key get --namespace-id=your-config-namespace-id",
    "kv:put": "wrangler kv:key put --namespace-id=your-config-namespace-id"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "monitoring",
    "cryptocurrency",
    "rates"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.114.14"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
